You are running a bounded DocGen page generation stage.

Page manifest entry:
{{PAGE_JSON}}

Delegate exactly this page to the `doc-writer` custom agent. The writer must read the declared evidence/model inputs and necessary supporting artifacts, then write exactly the manifest target path. Do not modify unrelated pages or application source. Validate Markdown structure before finishing.
