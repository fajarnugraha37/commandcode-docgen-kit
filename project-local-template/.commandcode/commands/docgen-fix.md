Repair exactly one documentation page with page id `$1` using its audit findings.

Delegate to the `doc-writer` custom agent. Read the manifest entry, evidence/model inputs, target page, and `.docgen/audit/pages/$1.json`. Fix supported findings without broadening scope, then leave the page ready for re-audit.
