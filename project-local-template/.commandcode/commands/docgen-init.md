Initialize the current repository with the globally installed DocGen engine.

Run the global `docgen init` CLI from the repository root and report the result. Do not manually recreate the workspace when the CLI is available.
