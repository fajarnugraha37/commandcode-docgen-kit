Run the evidence discovery workflow for scope `$ARGUMENTS` (use `.` when empty).

Delegate repository discovery to the `doc-discoverer` custom agent. The agent must read the DocGen evidence contract and relevant technology skills, produce or reconcile `.docgen/evidence/**`, and ensure `.docgen/evidence/index.json` is valid. Do not generate published documentation and do not modify application source.
