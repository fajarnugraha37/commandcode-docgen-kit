Run architecture synthesis for scope `$ARGUMENTS` (use all current evidence when empty).

Delegate to the `doc-architect` custom agent. Produce or reconcile `.docgen/model/**` and ensure `.docgen/model/system.json` is valid. Preserve FACT/INFERENCE/UNKNOWN distinctions and evidence references. Do not write `docs/**`.
